name: m4a3_styling_dex
description: Module 4 - Activity 3 - Styling the Dex (Flutter).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  # Live mobile preview while you run the app (a phone frame with a device
  # switcher). It does not affect grading.
  device_preview: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Used by the grader to photograph your screen inside a phone frame.
  device_frame: ^1.2.0

flutter:
  uses-material-design: true
